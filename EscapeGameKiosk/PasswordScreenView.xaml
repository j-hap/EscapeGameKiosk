<UserControl x:Class="EscapeGameKiosk.PasswordScreenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
  <Grid x:Name="PasswordScreenRoot"
        Background="#0B0B0B"
        TextElement.FontFamily="/EscapeGameKiosk;component/Fonts/#Orbitron"
        TextElement.Foreground="#E6E6E6">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>

      <Border Grid.Row="1"
              x:Name="PasswordPanelBorder"
              Background="#1C1C1C"
              BorderBrush="#3C3C3C"
              BorderThickness="2"
              Padding="36"
              CornerRadius="8"
              HorizontalAlignment="Center"
              VerticalAlignment="Center">
        <Border.RenderTransform>
          <TranslateTransform X="0" />
        </Border.RenderTransform>
        <StackPanel Orientation="Vertical"
                    Width="360">
          <TextBlock x:Name="AccessHeaderText"
                     Text="{Binding HeaderText, Mode=OneWay}"
                     Foreground="{Binding HeaderBrush, Mode=OneWay}"
                     FontSize="20"
                     FontWeight="Bold"
                     FontStretch="UltraExpanded"
                     HorizontalAlignment="Center"
                     Margin="0,0,0,18"/>
          <Grid Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*"/>
              <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid x:Name="PasswordInputContainer"
                  Grid.Column="0"
                  Height="36"
                  Margin="0,0,8,0">
              <PasswordBox x:Name="PasswordInputBox"
                           FontSize="16"
                           Style="{StaticResource OrbitronPasswordBoxStyle}"
                           Padding="10,0,30,0"
                           HorizontalContentAlignment="Center"
                           VerticalContentAlignment="Center"
                           PasswordChar="●">
                <PasswordBox.Template>
                  <ControlTemplate TargetType="PasswordBox">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="12">
                      <ScrollViewer x:Name="PART_ContentHost"
                                    HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                  </ControlTemplate>
                </PasswordBox.Template>
              </PasswordBox>
              <TextBox x:Name="PasswordRevealTextBox"
                       FontSize="16"
                       Padding="10,0,30,0"
                       HorizontalContentAlignment="Center"
                       VerticalContentAlignment="Center"
                       Visibility="Collapsed">
                <TextBox.Template>
                  <ControlTemplate TargetType="TextBox">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="12">
                      <ScrollViewer x:Name="PART_ContentHost"
                                    HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                  </ControlTemplate>
                </TextBox.Template>
              </TextBox>
              <ToggleButton x:Name="PasswordRevealToggle"
                            Width="92"
                            Height="28"
                            Margin="0,0,6,0"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            FontSize="10"
                            Style="{StaticResource OrbitronToggleButtonStyle}"
                            Foreground="#0B0B0B">
                <ToggleButton.Template>
                  <ControlTemplate TargetType="ToggleButton">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8">
                      <Grid>
                        <StackPanel x:Name="ShowState"
                                    Orientation="Horizontal"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Margin="6,0">
                          <TextBlock Text="👁"
                                     FontSize="12"
                                     Margin="0,0,6,0"
                                     VerticalAlignment="Center"/>
                          <TextBlock Text="SHOW"
                                     FontWeight="SemiBold"
                                     VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel x:Name="HideState"
                                    Orientation="Horizontal"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Margin="6,0"
                                    Visibility="Collapsed">
                          <TextBlock Text="🙈"
                                     FontSize="12"
                                     Margin="0,0,6,0"
                                     VerticalAlignment="Center"/>
                          <TextBlock Text="HIDE"
                                     FontWeight="SemiBold"
                                     VerticalAlignment="Center"/>
                        </StackPanel>
                      </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                      <Trigger Property="IsChecked"
                               Value="True">
                        <Setter TargetName="ShowState"
                                Property="Visibility"
                                Value="Collapsed"/>
                        <Setter TargetName="HideState"
                                Property="Visibility"
                                Value="Visible"/>
                      </Trigger>
                    </ControlTemplate.Triggers>
                  </ControlTemplate>
                </ToggleButton.Template>
              </ToggleButton>
            </Grid>
            <Button x:Name="UnlockButtonControl"
                    Grid.Column="1"
                    Content="➤"
                    Height="36"
                    Width="36"
                    FontSize="14"
                    Style="{StaticResource OrbitronButtonStyle}"
                    Command="{Binding UnlockCommand}"
                    IsEnabled="{Binding IsUnlockButtonEnabled, Mode=OneWay}">
              <Button.Template>
                <ControlTemplate TargetType="Button">
                  <Border Background="{TemplateBinding Background}"
                          BorderBrush="{TemplateBinding BorderBrush}"
                          BorderThickness="{TemplateBinding BorderThickness}"
                          CornerRadius="12">
                    <ContentPresenter HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                  </Border>
                </ControlTemplate>
              </Button.Template>
            </Button>
          </Grid>
        </StackPanel>
      </Border>
    </Grid>
  </Grid>
</UserControl>
